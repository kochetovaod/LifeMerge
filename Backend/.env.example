# Application Environment
# Allowed values: dev, stage, prod
# In stage/prod, secure secrets MUST be provided
APP_ENV=dev

# Database Configuration
DATABASE_URL=postgresql+asyncpg://lifemerge:lifemerge@db:5432/lifemerge

# ===== CORS CONFIGURATION =====
# Enable credentials (cookies, authorization headers)
CORS_ALLOW_CREDENTIALS=true

# List of allowed origins (JSON array or comma-separated)
# IMPORTANT: When CORS_ALLOW_CREDENTIALS=true, cannot use "*"
# For development:
CORS_ALLOW_ORIGINS=["http://localhost:3000", "http://localhost:8080", "http://127.0.0.1:3000"]

# For production example:
# CORS_ALLOW_ORIGINS=["https://app.lifemerge.com", "https://admin.lifemerge.com"]

# ===== SECURITY CRITICAL SETTINGS =====
# These MUST be changed in stage/prod environments!

# JWT Configuration
# In production: generate with: openssl rand -hex 32
JWT_SECRET_KEY=CHANGE_ME_IN_STAGE_AND_PROD

# Firebase Cloud Messaging
# Get from Firebase Console → Project Settings → Cloud Messaging
FCM_SERVER_KEY=CHANGE_ME_IN_STAGE_AND_PROD

# AI Service Internal Authentication
# Secure token for internal service communication
AI_SERVICE_AUTH_TOKEN=CHANGE_ME_IN_STAGE_AND_PROD

# ===== OPTIONAL SETTINGS =====
# Apple Push Notification Service (APNS)
# APNS_KEY_ID=
# APNS_TEAM_ID=
# APNS_KEY_PATH=

# Default Timezone
DEFAULT_TIMEZONE=UTC